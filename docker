
FROM node:14

# تعيين مجلد العمل داخل الحاوية
WORKDIR /usr/src/app

# نسخ ملف package.json و package-lock.json وتثبيت الوحدات
COPY package*.json ./
RUN npm install
RUN npx playwright install

# نسخ ملفات التطبيق
COPY . .

# تشغيل التطبيق عند بدء تشغيل الحاوية
CMD [ "node", "c.js" ]
